Some silly questions : 
1. Why do we write #ifdef __cplusplus
extern "C" {
#endif
#ifdef __cplusplus
}
#endif
#endif > what does it do ? 
2. Why does every function require a .h & .c  file combo ? 

For config.h - 
1. Why is the wifi connection timeout designed to be connected for 15 secs? why can't it stay connected all the time once it connects ? 
2.  How do we know MPU6050 i2c address is 0x68 ?
3. How do I make sure the task stack sizes are appropriate so there is no overflow ? assuming there are 9 tasks and each takes a 4096 stack size
4. in the Queue depths configs , are these all different queues ?   
5. Why are all threshold values written as ###f ? does the f signify float ? how does that work ? 
6. Why limit geofencing to 8 zones ? 
7. When a task tries to take a mutex and passes the max wait threshold and hence times out , what does timing out mean ? what happens to the task then ? 